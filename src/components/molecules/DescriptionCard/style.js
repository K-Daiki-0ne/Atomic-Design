import { makeStyles } from '@material-ui/core/styles';

export const useStyle = makeStyles({
  root: {
    backgroundColor: 'white'
  },
  text: {
    textAlign: 'center',
    fontSize: '24px',
    fontWeight: '400',
    color: '#000066'
  }
})