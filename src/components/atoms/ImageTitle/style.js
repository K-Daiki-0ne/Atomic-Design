import { makeStyles } from '@material-ui/core/styles';

export const useStyle = makeStyles({
  text: {
    textAlign: 'center',
    fontSize: '24px',
    fontWeight: '400',
    color: '#000066'
  }
})