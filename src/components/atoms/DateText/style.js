import { makeStyles } from '@material-ui/core/styles';

export const useStyle = makeStyles({
  text: {
    color: '#444444',
    textAlign: 'center',
    fontSize: '25px',
    fontFamily: 'sans-serif',
    fontWeight: '700'
  }
})