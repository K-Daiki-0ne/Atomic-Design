import { makeStyles } from '@material-ui/core/styles';

export const useStyle = makeStyles({
  text: {
    textAlign: 'center',
    fontSize: '24px',
    fontWeight: '600',
    color: '#444444',
    marginTop: '2%'
  }
})